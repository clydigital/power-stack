name: Live Desk context sync (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Live Desk pulse disabled
        run: echo "Power Stack currently uses data/macro-context.json. Live Desk context sync is intentionally disabled."
